# Ollama runs on Windows. With mirrored WSL networking localhost works;
# otherwise use the host IP: http://$(ip route show default | awk '{print $3}'):11434
OLLAMA_URL=http://localhost:11434   # or the IP Ollama is bound to on Windows
